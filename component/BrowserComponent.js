'use strict';

require('chai');
require('../utils/driver')

class BrowserComponent {

    containsInUrl(url){
        const contains = async url => {
            return expect()
        }
        return contains(url)
    }
}

module.exports = BrowserComponent;
